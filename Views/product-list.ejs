<!DOCTYPE html>
<html>
<head>
  <title>Products</title>
</head>
<body>
  <h1>Product List</h1>

  
  <ul>
    <% for(let p of prods) { %>
      <li>
        <a href="/products/<%= p.id %>">
          <%= p.title %>
        </a>
      </li>
    <% } %>
  </ul>
</body>
</html>